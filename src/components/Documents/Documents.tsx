import {Window} from '../../app/Window/Window';

export const Documents = () => {
	return (
	  <div>
		  <Window name="Documents" height={600} width={900}>
		  	<h1>Ils sont où mes écouteurs.</h1>
		  </Window>
	  </div>
	);
};